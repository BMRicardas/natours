doctype html
html
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport', content='width=device-width, initial-scale=1.0')
            link(rel='stylesheet', href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            link(href="https://api.mapbox.com/mapbox-gl-js/v3.10.0/mapbox-gl.css" rel="stylesheet")
            script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.18.0/axios.min.js', defer)
            script(src="https://api.mapbox.com/mapbox-gl-js/v3.10.0/mapbox-gl.js")
            script(src='https://js.stripe.com/v3/', defer)
            script(src='/js/alert.js', defer)
            script(src='/js/login.js', defer)
            script(src='/js/logout.js', defer)
            script(src='/js/mapbox.js', defer)
            script(src='/js/stripe.js', defer)
            script(src='/js/updateSettings.js', defer)
            title Natours | #{title}

    body
        //- HEADER
        include _header

        //- CONTENT
        block content
            h1 This is a placeholder heading

        //- FOOTER
        include _footer
